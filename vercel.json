{
  "framework": "vite",
  "buildCommand": "npm run build && node scripts/build-api.mjs",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.cjs"
    }
  ],
  "functions": {
    "api/server.cjs": {
      "maxDuration": 60
    }
  }
}
