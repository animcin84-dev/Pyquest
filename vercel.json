{
  "framework": "vite",
  "buildCommand": "npm run build && node scripts/build-api.mjs",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/handler.mjs"
    }
  ],
  "functions": {
    "api/handler.mjs": {
      "maxDuration": 60
    }
  }
}
